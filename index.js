/**
 * @format
 */

import {AppRegistry} from 'react-native';
import {name as appName} from './app.json';
import {MapsApp} from './src/presentation/MapsApp';

AppRegistry.registerComponent(appName, () => MapsApp);
